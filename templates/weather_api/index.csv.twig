city,country,date,celsius,fahrenheit
{% for measurement in measurements %}
{{ city }},{{ country }},{{ measurement.getDate().format('Y-m-d') }},{{ measurement.getCelsius() }},{{ measurement.getFahrenheit() }}
{% endfor %}
