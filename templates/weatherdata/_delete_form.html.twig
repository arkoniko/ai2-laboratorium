<form method="post" action="{{ path('app_weatherdata_delete', {'id': weatherdatum.id}) }}" onsubmit="return confirm('Czy na pewno chcesz usunąć ten element?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weatherdatum.id) }}">
    <button class="btn btn-danger btn-sm">Usuń</button>

</form>
